<?php

use Illuminate\Support\Facades\Route;

Route::group(['middleware' => [$AUTH$]], function () {
    Route::get('/cp/$ROUTES$', 'Web\$MODEL$Controller@index')
        ->name('cp.$ROUTES$.index');
});
